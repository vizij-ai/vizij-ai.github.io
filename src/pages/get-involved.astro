---
import BaseHead from "@/components/BaseHead.astro";
import SiteShell from "@/layouts/SiteShell.astro";
import GetInvolvedPage from "@/react-pages/get-involved/GetInvolvedPage";

const meta = {
  title: "Get Involved",
  description:
    "Get involved with Vizij - contribute, collaborate, and help build the open source ecosystem for expressive robot faces.",
};
const ogImage = "/og/get-involved.png";

// Volunteer opportunities section removed per specification.

// Donation impacts section removed per specification.

// Membership benefits section removed per specification.
---

<html
  class="overflow-x-hidden grid scroll-pt-18 scroll-smooth font-sans text-text text-xl md:text-base antialiased"
  lang="en"
>
  <head>
    <BaseHead
      title={meta.title}
      description={meta.description}
      ogImage={ogImage}
    />
  </head>
  <body class="relative min-h-screen w-full bg-surface">
    <SiteShell>
      <GetInvolvedPage />
    </SiteShell>
  </body>
</html>
