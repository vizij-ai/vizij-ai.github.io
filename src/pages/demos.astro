---
import SiteShell from "@/layouts/SiteShell.astro";
import DemosPage from "@/react-pages/demos/DemosPage";
import { siteConfig } from "@/site.config";

const meta = {
  title: "Vizij Demos",
  description:
    "Try Vizij's interactive runtime demos — rig controls, expressions, gaze, voice, and more for open source rendered robot faces.",
};
---

    <SiteShell
  title={meta.title}
  description={meta.description}
  ogImage="/og/demos.png"
>
      <DemosPage client:only="react" />
    </SiteShell>
  </body>
</html>
